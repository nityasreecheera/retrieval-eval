# Core — no dependencies needed for BM25, TF-IDF, hybrid, or evals
# All retrieval and evaluation logic uses Python stdlib + math only.

# Optional: LLM-powered question generation (Step 2)
# If OPENAI_API_KEY is set, the harness uses this to generate test questions
# from your corpus. Without it, the demo uses built-in sample questions.
openai>=1.0.0
